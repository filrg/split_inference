name: YOLO
server:
  cut-layer: a # or b, c, d
  clients:
    - 1
    - 1
  model: yolo26n
  batch-size: 4
rabbit:
  address: 127.0.0.1
  username: admin
  password: admin
  virtual-host: /

debug-mode: False
data: video.mp4
log-path: .
compress:
  enable: False
  num_bit: 8